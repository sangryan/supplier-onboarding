# Copy this to .env on the server and fill in real values
# cp .env.production.template .env

# Server Configuration
PORT=8000
NODE_ENV=production

# Database
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT Secret
# Generate with: openssl rand -base64 64
JWT_SECRET=
JWT_EXPIRE=7d

# Email — Resend SMTP relay (https://resend.com)
# Testing: use onboarding@resend.dev as FROM — works with no domain setup
# Production: verify betika.com in Resend dashboard, then change FROM to noreply@betika.com
EMAIL_HOST=smtp.resend.com
EMAIL_PORT=465
EMAIL_USER=resend
EMAIL_PASSWORD=re_eeWhPLnG_4kD1KLSZp8jaBWwXLzLeQw7T
EMAIL_FROM=onboarding@resend.dev

# Frontend URL
CLIENT_URL=http://41.191.225.34:5054

# File Upload
MAX_FILE_SIZE=20971520
UPLOAD_PATH=/app/uploads

# Company Details
COMPANY_NAME=Betika
COMPANY_EMAIL=procurement@betika.com
